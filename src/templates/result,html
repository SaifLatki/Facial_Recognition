{% extends 'base.html' %}

{% block content %}
<div class="card glass p-5 text-center">

    <h2 class="mb-4">Prediction Result</h2>

    <img src="{{ image_path }}" class="img-fluid rounded shadow mb-4" style="max-height: 300px;">

    <h3 class="text-success">{{ prediction }}</h3>
    <p class="lead">Confidence: {{ confidence }}%</p>

    <a href="/" class="btn btn-outline-light mt-3">Try Another Image</a>

</div>
{% endblock %}
